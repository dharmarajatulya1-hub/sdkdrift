{
  "name": "@sdkdrift/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "sdkdrift": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "echo 'lint not configured yet'"
  },
  "dependencies": {
    "@sdkdrift/core": "file:../core",
    "@sdkdrift/python-scanner": "file:../python-scanner",
    "commander": "^14.0.1"
  }
}
