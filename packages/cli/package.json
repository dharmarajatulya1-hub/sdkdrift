{
  "name": "@sdkdrift/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "sdkdrift": "dist/cli.js"
  },
  "main": "dist/cli.js",
  "types": "dist/cli.d.ts",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test",
    "lint": "echo 'lint not configured yet'"
  },
  "dependencies": {
    "@sdkdrift/core": "^0.1.0",
    "@sdkdrift/python-scanner": "^0.1.0",
    "commander": "^14.0.1",
    "js-yaml": "^4.1.0"
  }
}
