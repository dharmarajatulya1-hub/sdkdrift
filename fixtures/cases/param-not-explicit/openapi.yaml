openapi: 3.0.3
info:
  title: Param Not Explicit Case
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsers
      parameters:
        - name: after
          in: query
          required: false
          schema:
            type: string
      responses:
        "200":
          description: ok
