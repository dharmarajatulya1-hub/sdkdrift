openapi: 3.0.3
info:
  title: Verb Alias Modify Case
  version: 1.0.0
paths:
  /users/{id}:
    patch:
      operationId: modifyUser
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: ok
