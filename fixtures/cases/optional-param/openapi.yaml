openapi: 3.0.3
info:
  title: Optional Param Case
  version: 1.0.0
paths:
  /users:
    get:
      operationId: listUsers
      parameters:
        - name: cursor
          in: query
          required: false
          schema:
            type: string
      responses:
        '200':
          description: ok
